
const defaults = {
    options: [],
    items: [],
    valueField: 'id',
    labelField: 'text',
    searchField: ['text'],
    url: '',
    type: 'POST',
    defaultValue: '',
    maxOptions: 50,
    maxItems: 1,
    mode: '',
    showClear: true,
    checkboxMode: false,
    plugins: ['clear_button']
};

export default defaults;

